import db from "./queryHandler";
import QUERIES from "./queries";

export const checkEmailExists = (lookup, resolve, reject) => {
    
}

export const checkIdExists = (lookup, resolve, reject) => {
    
}

